import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const LayoutScreen = () => {
  return <Text style={{ fontSize: 30 }}>Layout</Text>;
};

LayoutScreen.navigationOptions = {
  title: 'Layout'
};

export default LayoutScreen;
